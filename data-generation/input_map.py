# Build .csv file linking images to labels

import glob, os

# Change this according to location of project
BASE_DIR = '/Users/jaevans/Desktop/COS435-project'
os.chdir(BASE_DIR)

for i in range(10):
	for file in glob.glob(sys.argvp1] + '/*.png'):
		print file + " " + str(0)

